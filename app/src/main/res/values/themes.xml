<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LockPhoto" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="splashTheme" parent="BaseAppTheme">
        <item name="postSplashScreenTheme">@style/lockTheme</item>
        <item name="android:windowBackground">@drawable/theme_splash_bg</item>
        <item name="windowSplashScreenBackground">@color/main_color</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_logo</item>
        <item name="windowSplashScreenAnimationDuration">2000</item>
    </style>

    <style name="Theme.LockPhoto.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.LockPhoto.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.LockPhoto.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="lockTheme" parent="BaseAppTheme">
        <item name="android:windowBackground">@color/main_color</item>
    </style>
</resources>